# 포트 설정
server:
  port: 8080

# mysql 연결
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop?serverTimezone=Asia/Seoul
    username: root
    password: 12341234
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  file:
    upload-dir: C:\superProfile
  jpa:
    hibernate:
      ddl-auto: create # 스키마 자동 생성
    show-sql: true # SQL 퀴리 출력
    properties:
      hibernate:
        format_sql: true # SQL pretty print
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: AN7ep_CD_ii1ftJlANpq
            client-secret: BxmrzjFAhZ
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope: name email profile_image
            client-name: supercoding
          kakao:
            client-id: acc3bcadd86193c806f6391bcad98507
            client-secret: tPH1IS0ZGwCxuMPUkr6OCl0ywlFSLltx
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            scope: profile_nickname account_email
            client-name: supercoding
